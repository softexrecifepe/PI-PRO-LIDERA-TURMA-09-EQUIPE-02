/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      serverActions: true, // Certifique-se de que está ativado
    },
  };
  
  export default nextConfig; // Para .mjs
  